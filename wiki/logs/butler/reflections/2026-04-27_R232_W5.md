# 反思 2026-04-27（round=232，第 7 次 W5，dream_round=否）

## 素材
- actions 最近 50 条（R181–R231），fail 0 条
- queue.md: P1=0 P2=几条discover | housekeeping_queue: 正常
- 上次反思：2026-04-27 R203（第 6 次 W5，Dream Round #2）

## 模式识别

### A 失败聚集
未发现。R203-R231 全 accept，0 fail。

### B 成功规则化
- **A6 fix-tags 已耗尽**：所有 202 页 tags≥4，该动作完成历史使命。下一阶段 tag 工作转为语义 tag 审查（已有内容的 tag 是否准确）而非补 tag。
- **enrich+H2+2PN 模式稳定**：每轮 +8-12 score，适合作为主力行动继续执行。
- **rename 原文引用 → 语义节名**：R207(叶文洁), R209(章北海), R214(太阳系) 均有效，应在 W2 中正式列为 enrich-page 的可选步骤（检测到"原文引用"节名时触发）。

### C 阈值
- queue.md P2 积压：少量 discover 任务（三体I/II/III corpus 扫描）
- stub 占比：0%
- housekeeping 积压：正常

### D 随机质量抽查
- 抽样页面：奥尔特星云、掩体纪元、面壁计划、太阳系、面壁者
- Q1–Q7 通过率：5/5（全部通过）
- 发现问题：无系统性问题
- 处理：无需操作

### E 跨项目借鉴

本次评估的 shiji-kb skills（按新度选取）：
1. **SKILL_W8_精品页建设方法论** — 定义 featured→premium 升级规范，分析高质量页面特征。三体适用性：**高**，100% featured 后自然进入 premium 建设阶段，时机成熟。→ **立即提案引入**。
2. **SKILL_W10l_引文PN一致性** — 系统性核验所有 PN 格式是否符合 `（B-CC-PPP）`。三体适用性：**高**，三体 wiki PN 密度大，且存在手写 PN 可能格式错误。移植成本：低（路径替换即可）。→ **记入 watchlist，下次 W5 决策**。
3. **SKILL_W10s_正文断言核验** — 验证页面中的事实断言是否有 corpus 依据。三体适用性：**中**，corpus_search 已做 L1 覆盖；正式流水线移植成本高。→ **记入 watchlist**。
4. **SKILL_W14_洞察发现** — 从语料中主动发现有趣关联/模式，生成"洞察页"。适用性：**低-中**，三体 wiki 目前重建档而非洞察生成。→ **已排除（当前阶段）**。
5. **SKILL_W9_页面图式反思** — 检查页面结构符合度。watchlist 中已有，202 页数量已满足触发条件（>100）。→ **升级为高优先级 watchlist**，考虑下下次 W5 引入。

### F 编辑错误

发现两类 F 类错误：

**F1：rebuild_recent.py 破坏多版本历史（R221 session）**
- 问题：在 session 恢复后，调用 `rebuild_recent.py` 将 recent.json 从 249 条压缩至 202 条（每页只保留最新版本），丢失 47 条历史修订记录。
- 根因：误以为 rebuild_recent.py 是安全的"重建"操作；实际上它是破坏性的"重置为每页最新"操作。
- 修复：已 `git checkout HEAD -- wiki/public/recent.json` 还原，并补录 record_revision.py。
- 规则：**`rebuild_recent.py` 为破坏性操作，严禁作为常规步骤调用。** 仅在 recent.json 文件完全损坏（无法解析）时才可使用。

**F2：record_revision.py 遗漏（R215-R216）**
- 问题：session 上下文恢复后，R215-R216 accept 时执行了 git add + record_action，但遗漏了 `record_revision.py` 调用。
- 根因：context 压缩时，W2 accept 工作流的必要步骤未在 session 初始化时再确认。
- 修复：后续补调 record_revision.py（内置 hash 去重，无副作用）。
- 规则：session 恢复后的第一轮，需补查 W2 accept 步骤清单。

### G 架构检查
- 总页面：202（与上次持平，无新建，进入纯维护/提升阶段）
- stub 占比：0%，featured 占比：100%（所有 65 非章节页）
- 待建：无（所有链接已解析）
- score 分布估计：低端约 53，高端约 68，中位约 60
- 无 G 类架构提案需要用户 review

## 修订清单（<=3 条）

1. **W2 禁令追加**：在 W2 accept 工作流中明确禁止 `rebuild_recent.py` 作为常规步骤，并在工具注意事项中标注其破坏性。
2. **W2 rename-section 常规化**：将 "检测原文引用节名 → 改为语义节名" 列为 enrich-page 的可选步骤（发现时必改）。
3. **W2 session-resume checklist**：在 W0 或 W2 中新增一条：session 恢复（上下文压缩重启）后，第一轮执行前必须确认 accept 工作流步骤清单（尤其是 record_revision.py）。

## 引入的借鉴 skill（不计入修订限额）
- [ ] 引入 SKILL_W8_精品页建设方法论：定义 premium tier（score≥65），为三体 wiki 下一阶段提供精品页建设规范。适配点：修改分数阈值、替换 corpus 路径。

## watchlist 更新
- 升级：SKILL_W9_页面图式反思 → 高优先级（已满足触发条件 pages>100）
- 新增：SKILL_W10l_引文PN一致性 → 中优先级
- 新增：SKILL_W10s_正文断言核验 → 低优先级（成本高）
- 排除：SKILL_W14_洞察发现（当前阶段不适用）

## quality_rules.md 追加
- QR-003：F1 类错误规则 — rebuild_recent.py 破坏性操作禁令

## 已排除
- SKILL_W14 — 三体当前阶段重建档，非洞察生成，时机未到
