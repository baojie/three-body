# 反思 2026-04-27（round=611，第 12 次 W5，dream_round=是）

## 素材
- actions 最近 50 条（R559–R608），fail 0 条
- queue.md: P1=0（全清），P2=少量（已处理）
- housekeeping_queue.md: H-P1=0，H-P2=少量（N问题已建）
- 上次反思：2026-04-27 R580（第 11 次 W5）
- 本轮触发：609 % 29 == 0（强制周期；实际领锁在 R611 因并发实例跳数）
- 是否 Dream Round：**是**（第 12 次，Dream 在 #3/#6/#9/#12）

---

## 模式识别

### A 失败聚集

无失败记录。整个 actions.jsonl 共 91 条 reflect，fail 0 条。创建流水线极稳定。

### B 成功规则化

1. **alias_index 验证**已成为 create-page 前置标准操作：R597/R599/R601 均描述了用 alias_index 过滤候选，防止创建已有别名页的流程。当前在 W2 中是隐性的，应显式化。

2. **multi-layer 语义扩展**已成为 discover_wanted 枯竭后的稳定备选路径（R597/R599/R602/R608 均触发），但仍以隐性操作进行，无正式记录。

### C 阈值

- queue.md P2 积压：0 条（全清）
- stub 占比：0（无存根）
- basic 占比：243/500 = 48.6%（高但正常——幸存者建页速度快于破壁人升级速度）
- featured 占比：254/500 = 50.8%（健康）
- 3 个 standard 页为三体I/II/III书册页（正常）

阈值无预警。create 与 enrich 的速率差距是设计行为（广度优先），不需调整。

### D 随机质量抽查

抽样：坦克 / 大气层 / 航空母舰 / 简并态 / 面壁法案

| 页面 | Q1 | Q2 | Q3 | Q4 | Q5 | Q6 | Q7 |
|------|----|----|----|----|----|----|-----|
| 坦克 | ✓ | ✓ | ✓ | ✓ | ✓(4) | ✓ | N/A |
| 大气层 | ✓ | ✓ | ✓ | ✓ | ✓(3) | ✓ | N/A |
| 航空母舰 | ✓ | ✓ | ✓ | ✓ | ✓(6) | ✓ | N/A |
| 简并态 | ✓ | ✓ | ✓ | ✓ | ✓(6) | ✓ | ✓ |
| 面壁法案 | ✓ | ✓ | ✓ | ✓ | ✓(9) | ✓ | ✓ |

通过率：5/5。无问题发现。

### E 跨项目借鉴

本次评估 watchlist 中 3 个已待决策项：

| skill | 功能 | 三体适用性 | 决策 |
|-------|------|-----------|------|
| `SKILL_W10c_词汇链接化` | 扫描正文中未 wikilink 的实体词，补充 `[[]]` | **高**——三体有 500 页，大量正文中提到的人名/词条未加链接 | **立即提案引入**（移植成本低：find_unlinked_entities.py，适配路径即可） |
| `SKILL_W10j_列表页建设` | 建立分类列表页（飞船列表/人物列表等） | **中**——已有500页但无飞船全列表/人物全列表 | 追加到 watchlist，条件：执剑人空闲时 |
| `SKILL_W9_页面图式反思` | 检查页面结构符合模板 | **中**——有 500 页，但 basic 页结构已标准化 | 继续 watchlist；当 basic 升级为主要任务时再引入 |

**决策：引入 SKILL_W10c 词汇链接化**——作为新内务任务 H20（已在 watchlist 标注为高优先级）。适配步骤：
1. 移植 find_unlinked_entities.py（扫描正文，比对 alias_index）
2. 输出：未链接实体列表 → 写入 housekeeping_queue.md H-P2
3. 成本：低（alias_index 已就位）

### F 编辑错误

1. **R593/R601/R606 label 冲突**：新建页面时 label 字段值若与已有页面的 id 或 alias 完全相同，build_registry 会触发冲突。例如 `label: 粒子加速器` 与已有 `id: 粒子加速器` 冲突。这是 A1 create-page 后置检查的盲点——当前只检查 id，不检查 label。

   → 写入修订提案（修订1）

2. **R604 魏程/量子通信内容错误**：量子通信.md 中引用了 "魏程" 但实际上语料中该词是 "魏程" 这个名字的误识别。H5 fix 已记录但未追踪执行。

   → 单页偶发，加入 housekeeping_queue.md H-P2 修复

### G 架构检查

- 总非章节页面：500（较 R580 的 ~450 页 +50 页）
- featured 占比：50.8%，目标 >50% ✓
- basic 占比：48.6%，代表升级空间充足
- stub 占比：0%
- discover 候选：discover_wanted.py 基本枯竭（broken links 均 1x 且 0 corpus），依赖语义扩展
- 顶层 broken links（1x）：窗口雪崩/太阳系毁灭/威慑后时代/面壁危机/宇宙学家/三星系统 — 均 0 corpus，暂缓

架构健康。唯一值得注意的是 **basic 页面积压（243 页）** ——破壁人升级速度低于幸存者建页速度。这是刻意设计（广度优先）但未来 6-12 月需要转向 enrich 专项。暂不提案（不是紧急问题）。

---

## Dream Round 分析（第 12 次，Dream #4）

### 扫描结果

- 总 reflect：91 条
- fail：0 条
- 高频主题（≥3次信号）：
  1. **label/alias 冲突**：R594, R601, R606 → 3 次明确描述 label≠id 触发冲突
  2. **discover_wanted 枯竭 + 语义扩展**：R597, R599, R602, R608 → 4 次

### 归纳问题

| 问题 | 信号 | 当前处理 |
|------|------|---------|
| label≠id 导致 build_registry 冲突 | 3次 | 手动发现后修复，无预防 |
| discover_wanted 枯竭后无正式备用路径 | 4次 | 每轮手动语义扩展，无规则记录 |

### Dream 提案

写入 dream_proposals.md。

---

## 修订清单（≤3 条）

### 修订1：A1 create-page 后置检查增加 label 冲突检测

**当前**：create-page 后运行 build_registry，若 id 与已有 id/alias 冲突则报错。但 label 字段不检查。

**修订**：在 W2 A1 的"后置检查"中增加：
> 若新页面 `label` 字段值不等于 `id`，且 `label` 值存在于 alias_index 中，记录警告并用 edit_page.py 修改 label 为 id（或与用户确认）。

**来源**：R594, R601, R606 均触发此问题。

### 修订2：W1 D1 discover 补充语义扩展层协议

**当前**：W1 描述 D1 discover 触发 discover_wanted.py，未说明枯竭时的处理。

**修订**：在 W1 的 D1 动作说明中追加：
> **枯竭模式**：当 discover_wanted --top 100 返回 <5 个有 corpus 命中的词条时，触发三层语义扩展：(1) 太空技术层（飞船/武器/设备类corpus词）；(2) 人物层（未建页的人名corpus命中）；(3) 概念层（宇宙/物理/社会学概念）。结果写入 queue.md P3，供下轮 create 消费。

**来源**：R597/R599/R602/R608 均描述了手动执行此流程。

---

## Dream Round 提案（不计入修订限额，需用户 review 后落地）

- [ ] 新动作 `H21: scan-label-conflicts`：扫描全库 pages，找出 label≠id 且 label 存在于 alias_index 的页面，批量修正 label=id（或记录为 H-P2 任务）。来源：R594/R601/R606 label 冲突 3 次，每次手动发现代价高。归入 W10 H 组，每 29 轮运行一次。
- [ ] 新动作 `D3: thematic-catalog`：当 discover_wanted 枯竭后，系统性地对语料库进行主题聚类（7-10 个主题组），每组 corpus_search 前 20 词条，生成 100-200 个候选页面写入 queue.md P3，预期寿命 20-30 轮。来源：R597/R599/R602/R608 发现枯竭后手动扩展效率低；D3 可提前一次性生成 buffer 而非每轮重复搜索。

---

## 引入的借鉴 skill（不计入修订限额）

- [ ] 引入 `SKILL_W10c_词汇链接化` → 三体版本 `H20: scan-unlinked-entities`：扫描每个页面正文，找未 wikilink 的实体词（在 alias_index 中有对应页面），批量补充 `[[]]`。移植步骤：(1) 复制 find_unlinked_entities.py 并适配 alias_index；(2) 在 W10 中添加 H20 任务规格；(3) 首次运行后写入 H-P2 队列消费。适配成本：低。

---

## quality_rules.md 追加

无新质量规则（D 类全通过）。

---

## 已排除

- R593 alias 冲突（弦论/弦理论）：已由 H4 merge-aliases 处理，非系统性错误，不写修订
- R604 魏程/量子通信：单页偶发内容错误，写入 H-P2 修复，不升为规则
